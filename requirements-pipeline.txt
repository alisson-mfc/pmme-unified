# Dependências apenas do pipeline local (anonimização + análise Claude + ML).
# NÃO são necessárias no Render — instale apenas em sua máquina:
#   pip install -r requirements.txt -r requirements-pipeline.txt

-r requirements.txt

google-genai>=2.0.0
wordcloud>=1.9.0
nltk>=3.9.0
scikit-learn>=1.5.0
matplotlib>=3.9.0
